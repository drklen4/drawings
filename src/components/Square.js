export default class Square {
    constructor(x, y, width) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = 100;
        this.color = `rgba(80, 80, 76, 0.58)`;
        this.visible = true;
    }
}